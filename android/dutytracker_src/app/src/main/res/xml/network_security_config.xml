<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Local LAN mode: allow HTTP (no HTTPS). -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
