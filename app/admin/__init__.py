"""
Blueprint для администраторских маршрутов.

Эти маршруты предоставляют доступ к статистике, спискам адресов и
заявок для панели администратора и телеграм‑бота. Они доступны
только для чтения и не требуют аутентификации (права
администратора проверяются в вызывающих функциях, если
необходимо).
"""

from compat_flask import Blueprint

# Создаём экземпляр Blueprint. Имя 'admin' будет использовано при
# регистрации маршрутов; префикс задаётся при регистрации в create_app.
bp = Blueprint('admin', __name__)

# Импортируем маршруты, чтобы они были зарегистрированы
from . import routes  # noqa: E402,F401