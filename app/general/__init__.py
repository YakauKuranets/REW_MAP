"""
Пакет general содержит общие API‑маршруты, не относящиеся к конкретному
блюпринту. Сюда вынесены операции импорта и экспорта списка адресов,
которые исторически располагались в корне `/api/export` и `/api/import`.

Эти эндпоинты оборачивают функции из модуля storage и адресного
блюпринта, позволяя вызывать экспорт и импорт без указания
`/addresses` в URL.  Блюпринт регистрируется с префиксом `/api`.
"""

from compat_flask import Blueprint

bp = Blueprint('general', __name__)

from . import routes  # noqa: F401
